# HCA audit baseline — 2026-07-12

> **Historical snapshot — superseded.** This audit describes the pre-modernization
> tree and is not current support, setup, or release guidance. Use
> [Current state](current-state-report.md), [Support matrix](support-matrix.md),
> and [Migration](migration.md). Do not execute cluster/model-launch commands
> quoted below.

## Environment

- Hermes: v0.18.2 (2026.7.7.2). Release tag commit `9de9c25f`; **installed
  upstream commit is now `f8ddf4fd`** — the `7b5ba205` recorded here was a
  historical main snapshot, not the release tag. Provenance is reconciled and
  probed live in [docs/upstream-compatibility.md](upstream-compatibility.md).
- Plan: docs/plans/2026-07-12-hermes-agent-modernization.md
- NVIDIA ops: https://github.com/NVIDIA/dgx-spark-playbooks

## Contracts pinned

> These string-pinned contracts are superseded by the **executable
> compatibility matrix** — capability probes in `src/hca/hermes_compat.py`,
> gated by `tests/contract/`, and reported by `hca doctor --json`. See
> [docs/upstream-compatibility.md](upstream-compatibility.md). The list below
> is retained as the historical baseline.

- `hermes_cli.kanban_db.dispatch_once(..., spawn_fn=...)` required
- Worker env: HERMES_KANBAN_TASK / RUN_ID / CLAIM_LOCK / BOARD / DB / WORKSPACE
- Run ownership: `Task.current_run_id` (integer) — mapped exactly, required
  before spawn (no more guessing `active_run_id` / `run_id` / `claim_run_id`)
- Worker shape: `hermes -p <slot> --cli --accept-hooks chat -q "work kanban task <id>"`
- tmux session names: `hca-<fleet>-<role>-NN` (no colons); concrete profile
  slot == tmux session == isolation identity
- Engines: vLLM (:8000) and SGLang (:30000) equal first-class
- Cluster: passwordless SSH after NVIDIA connect-* playbooks; no NFS SQLite

## Baseline commands (legacy still present)

- scripts/smoke-kanban-flow.sh PASS
- scripts/validate-docs.sh PASS
- scripts/health-monitor.sh fails on macOS (`free`) — replaced by hca doctor/telemetry path
